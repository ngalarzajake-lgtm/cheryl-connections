# Cheryl Connections

A dependency-free custom Connections game for Cheryl. Open `index.html` directly in a browser, or serve this directory with any static file server.

The first three category bands begin solved, while `WILL YOU`, `BE MY`, and `GIRLFRIEND` begin selected and locked. Cheryl only needs to tap `YES`, then `Submit`, to reach the celebration.
